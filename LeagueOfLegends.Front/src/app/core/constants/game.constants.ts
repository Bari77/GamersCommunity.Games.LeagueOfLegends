/** Slug of this game on the Platform, also used to scope what the browser remembers for it. */
export const LOL_GAME_ID = "league-of-legends";

/** Route prefix this remote is mounted on inside the shell. */
export const LOL_GAME_URL = `/${LOL_GAME_ID}`;
